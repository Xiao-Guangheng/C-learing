#include <iostream>
using namespace std;

template <typename T>
T max3(T a, T b, T c) {
    T m = a;
    if (b > m) m = b;
    if (c > m) m = c;
    return m;
}

int main() {
    cout << max3(3, 7, 5) << endl;
    cout << max3(1.2, 3.4, 2.1) << endl;
    return 0;
}